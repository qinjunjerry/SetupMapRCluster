---

version: 5

defaults:
  datadir  : hieradata
  data_hash: yaml_data

hierarchy:
  - name: "per node data"
    path: "nodes/%{facts.networking.fqdn}.yaml"

  - name: "common"
    path: "common.yaml"
